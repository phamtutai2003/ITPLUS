package backend;

import entity.EmployeeCommon;

public class EmployeeBackend extends EmployeeCommon {
    public EmployeeBackend(int id, String name, Number[] salaries) {
        super(id, name, salaries);
    }

    // Các phương thức và logic phía backend nếu cần
}
